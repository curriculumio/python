- Whole numbers are **int**s (short for *integer*)
- Precise numbers are **float**s (from the term *floating point number*)
- Sequences of characters are **string**s

The `type` function takes a value and returns its data type.
